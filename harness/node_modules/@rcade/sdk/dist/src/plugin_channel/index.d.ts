export declare class PluginChannel {
    private port;
    private channel;
    private pendingRequests;
    static acquire(name: string, version: string): Promise<PluginChannel>;
    private constructor();
    getPort(): MessagePort;
    getVersion(): String;
    /**
     * Send a request to the plugin and wait for a response.
     * The plugin must respond with a message containing the same `_nonce` field.
     */
    request<T = unknown>(message: Record<string, unknown>, timeoutMs?: number): Promise<T>;
}
//# sourceMappingURL=index.d.ts.map